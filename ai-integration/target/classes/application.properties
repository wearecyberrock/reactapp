# ai-integration application properties
# NOTE: Do NOT commit your API key. Provide it at runtime via an environment variable.
# Example (Git Bash): export GEMINI_API_KEY="your_key_here"
# Example (PowerShell): $env:GEMINI_API_KEY = "your_key_here"

# Server port (optional)
server.port=8080

# The spring-ai starter will typically read provider credentials from env vars or
# standard cloud configuration. Ensure you set the GEMINI_API_KEY environment
# variable before starting the app.
